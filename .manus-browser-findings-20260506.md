# 本轮真实页面验证记录

## 页面一：`/projects/90001/designs`
- 初次实测时，结果页仅出现旧 6 套方案：赛博朋克、未来科技、暗黑竞技、极简电竞、复古电竞、潮流酒吧。
- 修复后再次在真实页面触发“生成5-10种设计方案”，结果页已出现第 7 套方案 **Party K (`party_k`)**。
- 当前真实页面已能看到 7 套方案：`cyberpunk`、`future_tech`、`dark_gaming`、`minimalist`、`retro_gaming`、`trendy_bar`、`party_k`。

## 页面二：`/projects/90001/design/390049`
- 进入 Party K 设计详情页后，切换到“历史图片”标签页，无需返回主页或手动刷新即可直接看到历史图片卡片。
- 历史图片卡片中可见多个区域与旧设计记录，例如卫生间、包间、散座、舞台、吧台、通道等。
- 每张历史图片卡片带有下载入口，说明项目级聚合历史记录链路已在真实页面可见。

## 页面三：历史图片占位图状态标识验证
- 在 `/projects/90001/design/390049` 重新生成当前效果图后，真实页面已出现 toast 与内联提示：说明当前图像服务额度已用尽，系统自动展示占位图。
- 切换到“历史图片”标签页后，最新的历史记录卡片已显示“占位图”标签，并出现说明文案：该历史记录为 fallback 占位图，图像服务恢复后可重新生成真实图片。
- 该标识可防止用户把 fallback 预览误认为真实渲染结果。

## 页面四：图片加载与 WebSocket 根因排查
经日志与接口返回核对，当前 `/api/trpc/designs.get,renderings.list` 已直接返回 `data:image/svg+xml;base64,...` 形式的占位图 URL，说明当前页面展示的问题主因不是图片请求链路被连接重置，而是图像服务在生成阶段返回了 `usage exhausted`，后端已回退为可加载的占位图。

本轮未在最近浏览器日志中发现新的 HMR WebSocket 报错记录；当前页面上的图片已经能够稳定显示为占位图，不再是纯破图或仅剩 alt 文本。

## 页面五：HMR WebSocket 复发问题复测
移除 `vite.config.ts` 中强制的 `server.hmr.protocol = "wss"` 与 `clientPort = 443` 后，重启开发服务并重新打开设计详情页，浏览器控制台最新记录已变为 `[vite] connected.`，且在本轮新会话时间段内未再追加新的 `WebSocket closed without opened` 报错。

这说明近期复发的 HMR WebSocket 问题来自固定的远端 HMR 覆盖配置，与当前效果图展示为占位图的主因不同；图片问题仍由图像服务 `usage exhausted` 触发。

## 页面六：旧设计详情页图片渲染复核
对 `/projects/60002/design/360004` 再次进行页面取证，当前视图可见 7 张区域卡片均已实际渲染在页面中，卡片内可见对应的占位预览图块与区域标题，而不是破图图标或只剩 alt 文本。

结合 `renderings.list` 返回的 `data:image/svg+xml;base64,...` URL，可以确认该页面目前属于“可稳定显示占位图”的状态，不存在新增的 `ERR_CONNECTION_RESET` 图片加载失败表现。

## 页面七：内置图像服务直接探测
使用当前环境里的 `BUILT_IN_FORGE_API_URL` 与 `BUILT_IN_FORGE_API_KEY` 直接向 `images.v1.ImageService/GenerateImage` 发起最小 POST 请求，服务立即返回 `HTTP 400`，响应体为：`{"code":"failed_precondition","message":"your account has hit a usage exhausted"}`。

这表明 `usage exhausted` 并不是前端参数拼装、tRPC 路由封装或 S3 存储写入造成的，而是在 forge 图像服务入口就已经发生；同时返回头中的 `x-ratelimit-remaining: 199` 说明它不是普通的每分钟请求次数耗尽，更像是账号级图像额度或 provider 侧配额耗尽。

## 页面八：模型与 provider 切换能力核对
项目内与图像生成相关的实现只在 `server/_core/imageGeneration.ts` 中向固定的 `images.v1.ImageService/GenerateImage` 发起请求，请求体仅包含 `prompt` 与 `original_images`，当前代码中不存在 `model`、`provider` 或备用图像服务路由的可配置入口。

因此，在不更换平台侧图像额度/服务账户、也不新增另一条可用图像生成通道的前提下，应用内目前没有可通过改参数立即恢复真实出图的现成修复路径。

## 页面九：交付包下载验证
在 `/projects/90001/design/390049` 点击“下载交付包”后，页面即时出现“交付包已开始下载，已包含当前效果图、预算报表与元数据说明”的提示。随后在 `chrome://downloads` 中确认已生成文件 `design-390049-Party-K-delivery-package.zip`，说明前端 JSZip 打包与浏览器下载链路已正常工作。

## 页面十：单图 JPG / PNG 导出验证
在 `/projects/90001/design/390049` 的当前效果图查看器中，已经可以看到新增的“下载 PNG”和“导出 JPG”按钮。点击后页面分别出现“已开始下载PNG”“已开始下载JPG”的提示，说明两条单图导出链路在真实页面中均已打通。
